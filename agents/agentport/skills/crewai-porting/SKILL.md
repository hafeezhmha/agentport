# CrewAI Porting

Use deterministic evidence from `agents.yaml`, `tasks.yaml`, and Python AST calls to `Agent(...)`, `Task(...)`, and `Crew(...)`.

Port roles, goals, backstories, task descriptions, expected outputs, model preferences, and static tool metadata. Flag Crew runtime process settings, callbacks, and implementation code for manual review.
