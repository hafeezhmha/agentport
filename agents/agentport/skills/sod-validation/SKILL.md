# SOD Validation

Check that maker, checker, publisher, and learner responsibilities remain distinct.

Reject workflows where the schema writer validates its own output or where PR preparation bypasses validation failures.
