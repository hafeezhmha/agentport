# Migration Reporting

Every migration report must state: "This migration ports the agent identity layer, not the full runtime implementation."

Include source path, framework detection evidence, generated files, validation status, and manual review items.
