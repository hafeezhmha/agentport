# Claude/Cursor Porting

Extract identity from `CLAUDE.md`, `AGENTS.md`, `.cursorrules`, and `.cursor/rules/*.mdc`.

Port repository rules, coding constraints, style instructions, workflow expectations, and output format requirements.
