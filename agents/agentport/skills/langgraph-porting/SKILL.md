# LangGraph Porting

Extract prompts, chat templates, role instructions, model preferences, and tool metadata from LangGraph/LangChain repos.

Flag graph topology, state machines, checkpointing, memory stores, async node execution, retrievers, and runtime callbacks for manual review.
