# LangChain Porting Notes

LangChain repos can spread identity across prompt templates, tool classes, chain setup, retrievers, and agent executor configuration.

Port prompt templates, instructions, model preferences, and tool metadata. Flag retrievers, vector stores, callbacks, and execution chains for manual review.
