# Claude/Cursor Porting Notes

Claude and Cursor instruction repos usually keep portable identity in `CLAUDE.md`, `.cursorrules`, `.cursor/rules/*.mdc`, `AGENTS.md`, and related Markdown files.

Port repository rules, style guidance, constraints, workflow instructions, and output expectations.
