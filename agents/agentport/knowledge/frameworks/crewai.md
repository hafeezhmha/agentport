# CrewAI Porting Notes

CrewAI often stores identity in `agents.yaml`, `tasks.yaml`, and Python `Agent(...)` or `Task(...)` calls.

Port roles, goals, backstories, task descriptions, expected outputs, and static tool metadata. Flag crew execution, process settings, decorators, callbacks, and tool implementation code for manual review.
