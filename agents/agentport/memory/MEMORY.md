# AgentPort Memory

- Port identity layers before runtime implementation.
- Prefer deterministic extraction from files, YAML/JSON, Markdown, and Python AST.
- Keep validation and readiness separate from generation.
